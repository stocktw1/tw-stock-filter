runtime: python311
instance_class: F2

entrypoint: gunicorn --bind :$PORT --workers 2 --threads 8 --timeout 120 server:app

automatic_scaling:
  min_instances: 0
  max_instances: 2
  target_cpu_utilization: 0.65

handlers:
  - url: /static
    static_dir: static

  - url: /.*
    script: auto
