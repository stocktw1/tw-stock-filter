# 專案特定規則 (Workspace Rules)

## 磁碟安全與資料庫讀寫限制
* **D 磁碟 (D:\) 唯讀限制：**
  * `D:\` 磁碟上的所有檔案與資料庫（包含 `D:\csv資料庫` 等所有子目錄）**一律僅供讀取（Read-Only），嚴禁進行任何寫入、修改、覆蓋或刪除操作**。
  * 任何產生的資料、重構的股票數據或輸出結果，必須存放於本專案目錄（如 `stockdata/`）或指定之本機應用程式目錄中。
